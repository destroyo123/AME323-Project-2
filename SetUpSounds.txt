Hi Maren! Open a 'terminal' window
In terminal, navigate to the folder of this repo
by running this:

cd "your filepath to this folder"

and run these commands in that terminal:

and run:

chmod +x .githooks/post-merge
chmod +x scripts/play-sound.sh

This makes these scripts executable on MacOS...

Then also BOTH OF US need to run this
in our copy of the repo root

git config core.hooksPath .githooks